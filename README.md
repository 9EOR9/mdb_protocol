# mdb_protocol

This is module is a proof of concept and not intended for use in production.

It offers some helper function for MariaDB/MySQL client server protocol as
a replacement for Python's struct module.

At the moment only unpack functionality for the text protocol is supported.
